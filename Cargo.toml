[workspace]

members = ["derive"]

[package]
name = "enumorph"
version = "0.1.2"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
enumorph-derive = { path = "derive" }

[dev-dependencies]
trybuild = "1.0.85"
